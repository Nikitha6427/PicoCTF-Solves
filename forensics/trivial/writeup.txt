first we analyse using wireshark then we filter the tftp packets using tftp.type then we go to file > expot objects > as tftp then we dowload it in our folder and get a few files we cat instructs and rot13 then followed by plan and we then find dudediligence in caps in plan file. so we try to install the given program and figure out it to be steghide we use the command steghide etract -sf filename -p password that id dudediligence. we get flag.txt finallly
